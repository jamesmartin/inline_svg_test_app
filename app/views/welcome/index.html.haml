%h1 Inline SVG Tests
%p You'll find all SVGs in app/assets/images/

%section
  %h2 Basic SVG With no Options
  .svg
    = inline_svg('iconmonstr-glasses-12-icon.svg')
  %h2 SVG With CSS Class
  .svg
    = inline_svg('iconmonstr-glasses-12-icon.svg', class: 'little-red')
  %h2 SVG With ID Attribute
  .svg
    = inline_svg('iconmonstr-glasses-12-icon.svg', id: 'some-id')
  %h2 SVG With Data Attributes
  .svg
    = inline_svg('iconmonstr-glasses-12-icon.svg', data: {some: "value", another: "thing"})
  %h2 SVG With Title and Description
  .svg
    = inline_svg('iconmonstr-glasses-12-icon.svg', class: 'medium-blue', title: 'An SVG', desc: 'This is my SVG. There are many like it. You get the picture')
  %h2 SVG With Size (Height & Width) Attribute
  .svg
    = inline_svg('iconmonstr-glasses-12-icon.svg', size: '15% * 20%')
  %h2 SVG With Separate Width and Height Attributes
  .svg
    = inline_svg('iconmonstr-glasses-12-icon.svg', width: '15%', height: '20%')
  %h2 Size, Width & Height
  %p Height and Width always take precedence over Size.
  .svg
    = inline_svg('iconmonstr-glasses-12-icon.svg', size: '15% * 20%', width: '25%')
  %h2 SVG With Comments Removed
  .svg
    = inline_svg('iconmonstr-glasses-12-icon.svg', class: 'medium-yellow', title: 'An SVG, scrubbed', desc: 'This is my SVG. There are many like it. You get the picture', nocomment: true)
  %h2 Missing SVG File
  %p (Check the page source to see the output)
  .svg
    = inline_svg('some-missing-file.svg')
  %h2 Preserve Aspect Ratio
  .svg
    = inline_svg('iconmonstr-glasses-12-icon.svg', preserve_aspect_ratio: 'xMaxYMax meet')
  %h2 Custom Transformation
  .svg
    %p (Check the page source to see the 'custom' attribute attached to the SVG)
    = inline_svg('iconmonstr-glasses-12-icon.svg', custom: 'some value')
